import { Config, Env } from '../commands/args';
export interface BabelRC {
    presets: any[];
    plugins: any[];
    cacheDirectory?: boolean;
    babelrc?: boolean;
}
/**
 * Exporting .babelrc config used on docz
 */
export declare const getBabelConfig: (args: Config, env: Env) => Promise<BabelRC>;
