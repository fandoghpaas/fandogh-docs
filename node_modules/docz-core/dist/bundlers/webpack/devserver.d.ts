import * as Koa from 'koa';
import { Configuration } from 'webpack';
import { Config } from '../../commands/args';
import { ServerHooks } from '../../Bundler';
export declare const devServerConfig: (args: Config, config: Configuration, hooks: ServerHooks) => {
    config: Configuration;
    host: string;
    port: number;
    content: string[];
    logLevel: string;
    devMiddleware: {
        logLevel: string;
    };
    hotClient: {
        reload: boolean;
        logLevel: string;
        host: string;
        port: number;
    };
    add: (app: Koa, middleware: any, options: any) => void;
};
