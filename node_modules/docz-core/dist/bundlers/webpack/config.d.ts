import { Configuration } from 'webpack';
import { Config as Args, Env } from '../../commands/args';
import { BabelRC } from '../../utils/babel-config';
export declare const createConfig: (args: Args, env: Env) => (babelrc: BabelRC) => Configuration;
