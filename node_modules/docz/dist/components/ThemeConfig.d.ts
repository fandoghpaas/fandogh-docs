import { ReactNode, SFC } from 'react';
import { ThemeConfig as ThemeConfigObj } from '../state';
export interface ThemeConfigProps {
    children?: (config: ThemeConfigObj) => ReactNode;
}
export declare const configSelector: any;
export declare const ThemeConfig: SFC<ThemeConfigProps>;
